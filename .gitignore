# Secrets — never commit
.env
.env.local
.env.production

# Dependencies
node_modules/
netlify/functions/node_modules/

# Netlify CLI local scratch (machine-specific, never commit)
.netlify/

# Logs & OS/editor noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Corpus working files (review.json IS committed — it's the curation record)
corpus/rejected.json
